"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class Plane(object):

    __slots__ = ["coef"]

    __typenames__ = ["double"]

    __dimensions__ = [[4]]

    def __init__(self):
        self.coef = [ 0.0 for dim0 in range(4) ]
        """ LCM Type: double[4] """

    def encode(self):
        buf = BytesIO()
        buf.write(Plane._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack('>4d', *self.coef[:4]))

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Plane._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Plane._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = Plane()
        self.coef = struct.unpack('>4d', buf.read(32))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if Plane in parents: return 0
        tmphash = (0x7e294b30ea2123f2) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if Plane._packed_fingerprint is None:
            Plane._packed_fingerprint = struct.pack(">Q", Plane._get_hash_recursive([]))
        return Plane._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Plane._get_packed_fingerprint())[0]

