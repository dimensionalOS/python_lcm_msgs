"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

from . import *
from .Vector3 import Vector3
class Accel(object):

    __slots__ = ["linear", "angular"]

    __typenames__ = ["Vector3", "Vector3"]

    __dimensions__ = [None, None]

    def __init__(self):
        self.linear = Vector3()
        """ LCM Type: Vector3 """
        self.angular = Vector3()
        """ LCM Type: Vector3 """

    def encode(self):
        buf = BytesIO()
        buf.write(Accel._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.linear._get_packed_fingerprint() == Vector3._get_packed_fingerprint()
        self.linear._encode_one(buf)
        assert self.angular._get_packed_fingerprint() == Vector3._get_packed_fingerprint()
        self.angular._encode_one(buf)

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Accel._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Accel._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = Accel()
        self.linear = Vector3._decode_one(buf)
        self.angular = Vector3._decode_one(buf)
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if Accel in parents: return 0
        newparents = parents + [Accel]
        tmphash = (0x3a4144772922add7+ Vector3._get_hash_recursive(newparents)+ Vector3._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if Accel._packed_fingerprint is None:
            Accel._packed_fingerprint = struct.pack(">Q", Accel._get_hash_recursive([]))
        return Accel._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Accel._get_packed_fingerprint())[0]

